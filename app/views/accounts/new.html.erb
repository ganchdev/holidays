<h1>Create New Account</h1>

<%= form_with model: Current.user.account do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "Create Account" %>
  </div>
<% end %>

<%= link_to "Back", root_path %>
