<% content_for :title, "New booking" %>

<h1>New booking</h1>

<%= render "form", booking: @booking %>

<br>

<div>
  <%= link_to "Back", property_room_bookings_path(@property, @room) %>
  <%= link_to "Back to bookings", property_room_bookings_path(@property, @room) %>
</div>
