#!/usr/bin/env ruby
# frozen_string_literal: true

# Change to the project root directory
Dir.chdir(File.expand_path("..", __dir__))

# Execute steep check through bundler
exec "bundle", "exec", "steep", "check", *ARGV
